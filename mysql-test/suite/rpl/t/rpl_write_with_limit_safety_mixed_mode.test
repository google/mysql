--source include/have_binlog_format_mixed.inc

# the actual test is the same as for rpl_limit_query_stmt_warnings so is
# moved to an include:
--source ../include/rpl_write_with_limit_safety_test.inc
